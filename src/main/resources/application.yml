
server:
  port: ${PORT:8080}

spring:
  application:
    name: ORDER-SERVICE

  datasource:
    url: jdbc:postgresql:///order_db?cloudSqlInstance=global-baton-502904-d7:asia-south1:order-db-instance&socketFactory=com.google.cloud.sql.postgres.SocketFactory
    username: postgres
    password: 4UxS_tl$\cx~s;/5
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    enabled: false