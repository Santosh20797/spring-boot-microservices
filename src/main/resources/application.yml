server:
  port: ${PORT:8080}

spring:
  application:
    name: ORDER-SERVICE

  datasource:
    url: jdbc:postgresql://google/order_db?socketFactory=com.google.cloud.sql.postgres.SocketFactory&cloudSqlInstance=global-baton-502904-d7:asia-south1:order-db-instance
    username: postgres
    password: 4UxS_tl$\cx~s;/5
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    enabled: false